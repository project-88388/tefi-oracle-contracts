import { Command } from '@oclif/command';
export default class SyncRefs extends Command {
    static description: string;
    static flags: {
        'refs-path': import("@oclif/command/lib/flags").IOptionFlag<string>;
        dest: import("@oclif/command/lib/flags").IOptionFlag<string>;
    };
    run(): Promise<void>;
}
