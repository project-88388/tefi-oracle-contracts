import { LCDClient, Wallet } from '@terra-money/feather.js';
export declare const getSigner: ({ network, signerId, keysPath, lcd, prefix, }: {
    network: string;
    signerId: string;
    keysPath: string;
    lcd: LCDClient;
    prefix: string;
}) => Promise<Wallet>;
