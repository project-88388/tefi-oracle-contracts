import { APIParams } from '../APIRequester';
import { LCDClient } from '../LCDClient';
import { BaseAPI } from './BaseAPI';
import { QueryParamsResponse } from '@terra-money/terra.proto/cosmwasm/wasm/v1/query';
import { AccAddress } from '../../../core';
export interface DenomsFromCreatorResponse {
    denoms: string[];
}
export interface AuthorityMetadataResponse {
    authority_metadata: {
        admin: AccAddress;
    };
}
export declare class TokenFactory extends BaseAPI {
    lcd: LCDClient;
    constructor(lcd: LCDClient);
    params(chainID: string, params?: APIParams): Promise<QueryParamsResponse>;
    denomsFromCreator(creator: AccAddress, params?: APIParams): Promise<DenomsFromCreatorResponse>;
}
